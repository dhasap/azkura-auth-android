<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <!-- Azkura stores TOTP secrets locally. Keep Android system cloud/device-transfer
         backup disabled; Google Drive export/restore is handled explicitly in-app. -->
    <!-- Note: DataStore/Preferences files live under the app's files/ dir, so the
         "file" domain exclusion below already covers them. There is no separate
         "datastore" backup domain (using one fails release lint / FullBackupContent). -->
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
